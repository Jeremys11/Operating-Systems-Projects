Running Code from home directory:

    Recommended on Bash Terminals on Linux/MacOS
    Should work on Windows

    Use to run bash testing file:
        bash test.sh

        Test file creates, writes, reads, and drops on token to test for correctness.
        Tests adding a preexisting token, writing, reading, and dropping a token to test error detection.

Running Server:

    Starting Server:
        go run tokenserver/tokenserver.go -port <STRING>

Running Client:

    Create:
        go run tokenclient/tokenclient.go -create -id <STRING> -host <STRING> -port <STRING>

    Write:
        go run tokenclient/tokenclient.go -write -id <STRING> -name <STRING> -low <UINT64> -mid <UINT64> -high <UINT64> -host <STRING> -port <STRING>

    Read:
        go run tokenclient/tokenclient.go -read -id <STRING> -host <STRING> -port <STRING>

    Drop:
        go run tokenclient/tokenclient.go -drop -id <STRING> -host <STRING> -port <STRING>


Possible Errors:

    Need to recompile runserver:

        export PATH="$PATH:$(go env GOPATH)/bin"

        protoc --go_out=. --go_opt=paths=source_relative \
        --go-grpc_out=. --go-grpc_opt=paths=source_relative \
        runserver/runserver.proto

Unarchive:

    tar -xvzf Project2.tar.gz